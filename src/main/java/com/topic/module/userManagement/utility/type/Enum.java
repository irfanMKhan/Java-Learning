package com.topic.module.userManagement.utility.type;

public class Enum {
    public enum LogType {
        INFO,
        REQUEST,
        RESPONSE,
        DB_OPERATION,
        DB_EXCEPTION,
        SERVICE_EXCEPTION,
        BACKGROUND_EXECUTION,
        ACTIVITY,
        EVENT,
        EVENT_EXCEPTION
    }
}
