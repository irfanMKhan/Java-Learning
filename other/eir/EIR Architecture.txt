







	Client request (Check IMEI)-> Network -> DEIR
											 DEIR-> Check IMEI in Database
												  Exist->Return Result(Black=1/White=0/Gray=0)
												  Non Exist -> NEIR




    Client request type-> Networtk -> 2G/3G (Sigtran)
	                               -> 4G    (Diameter)
				   
    
    Main Data Flow

    Main Appliction -> DB
	                   DB-> SP_Get Identity Status(Procedure)-> Present in DB -> result(0/1)
                                                             -> Absent in DB ->Proceed to NEIR(Default return 0)		                                         
					  
	
	
	
	
	NEIR Request FLow
	
	Proceed to NEIR-> Insert in TBL_NEIR_REQ_QUEUE_REG Table -> EIR_SP_Executor_Preproc_2nd( fetch 3000 data in every 30 second to TBL_NEIR_REQ_QUEUE_REG_2nd )
	
	
	TBL_NEIR_REQ_QUEUE_REG_2nd -> NEIRRequestPreprocessor_REG-> Check MSSDN -> Yes -> Insert in TBL_NEIR_REQ_QUEUE_REG_FN Table 
	
	                                                                        ->  No -> Insert in TBL_IMSI_MSISDN_MAPPING_QUEUE Table
																			
																			
	TBL_IMSI_MSISDN_MAPPING_QUEUE ->  dCRMClient_Service(MSSDN Update)      -> Insert in TBL_NEIR_REQ_QUEUE_REG_FN Table 
	
	TBL_NEIR_REQ_QUEUE_REG_FN     ->  NEIRRequester_REG(Returns request id)
	
	
	
	NEIR Response
	
	NEIR Response API -> TBL_API_RESPONSE_SERVICE -> APIResponseProcessor_Service ->Result(WL/BL)
	
	WL -> Insert in TBL_IMEI_White_List 
	BL -> SMS_CLIENT_SERVICE(send sms) -> Insert in TBL_OUTBOX_HLR -> HLRClient_Service_Robi(Cancel Location )
	                                      Insert in TBL_IMEI_Black_List (Block IMEI)
	
	
	